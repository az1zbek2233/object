// let azizarr = ["Aziz", 15 , "tinchlik"];

// console.log(`${azizarr[0]} ${azizarr[1]} da ${azizarr[2]} da turadi`);

let ism = prompt("ismingizni kiriting");
let id = Number(prompt("id kiriting"));
let password = Number(prompt("password kiriting"));

let azizobj = {
    name: ism,
    id: id,
    password: password
};

console.log(azizobj);